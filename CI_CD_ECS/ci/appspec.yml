version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "REPLACE_WITH_TASK_DEF_ARN"
        LoadBalancerInfo:
          ContainerName: "portfolio-container"
          ContainerPort: 8000
Hooks:
  AfterAllowTestTraffic:
    - location: CI_CD_ECS/validation_lambda.sh
      timeout: 300

